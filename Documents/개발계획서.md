# 개발계획서

ConExe 를 도입하며 ConBox 를 **풀스크린 터미널 에뮬레이터**로 키우는 다단계 작업의
로드맵과 진행상황을 담는다. 여러 세션에 걸쳐 진행되므로, 세션이 끊겨도 다음 세션이
**이 문서의 진행상황부터** 이어받도록 `CLAUDE.md` 에서 import 한다.

- 이 문서는 "살아있는 진행 문서"다. 각 마일스톤이 끝나면 그 결과(확정 API·동작·구조·
  검증)를 정식 문서(요구사양서 §3 · 설계명세서 §2 · 개발노트 §2)로 이관하고, 여기에는
  완료 표시만 남긴다.

---

## 진행상황 (다음 세션은 여기부터)

- [x] 세 문서(요구/설계/개발노트)를 "1.개요 / 2.ConBox / 3.ConExe" 장 구조로 개편
- [x] ConExe 아키텍처 결정 (아래 "확정 결정" 표)
- [x] 개발계획서 정착 + `CLAUDE.md` import
- [x] **M1 — ConPTY 왕복(출력 단방향)** — `Source/ConExe.h/.cpp`(CConExe) 신설, 데모에서
      `cmd /c dir` 출력이 ConBox 에 표시됨을 캡처로 확인(VT 미해석 상태). vcxproj/filters 등록.
- [x] **M2 — 입력 raw 전송 (왕복 완성)** — ConBox 에 입력 싱크(`set_input_sink`) 추가로 터미널
      모드 도입(로컬 편집·에코 끔, 키→VT/UTF-8 인코딩). ConExe `write()`+`attach()` 가 양방향
      연결. 데모를 상호작용 `cmd` 로 바꿔 `dir`+Enter 왕복 실행을 캡처로 확인. 한글 IME 는
      터미널 모드에서 확정분만 자식에 전송(조합 순서 보장은 M5, 요구사양서 3.1).
- [~] **M3 — 셀 그리드 화면 버퍼 + 코어 VT 파서** (가장 큰 단계, 3a/3b/3c 로 분할 진행)
  - [x] **M3a — 셀 그리드 전환 (출력/렌더, VT 미해석)** — ConBox 내부를 append 모델
        (`log_buf`/`scr_lines`)에서 `lines`(셀 그리드 물리 줄 + 스크롤백) + 커서 `(cur_row,cur_col)`
        절대 위치로 교체. `print`/`OnPaint`/스크롤을 그리드 모델로 재작성. 로컬 줄편집·`on_enter`·
        IME 삽입·마우스 선택 경로는 제거(터미널 전용화). 데모에서 `cmd` 출력이 그리드에 흐르고
        초기 도표가 회귀 없이 보임을 캡처로 확인(VT 미해석, M1/M2 와 같은 그림). 선택/복사 복구는
        후속 3a-2.
  - [x] **M3b — 화면 셀 그리드 모델 + 코어 VT 파서** — append 유사 모델(`lines`)을 고정 화면
        그리드(`screen` rows×cols + `scrollback` + **칸 좌표 커서**)로 전환하고 `print` 를 VT 파서
        (`vt_feed`/`dispatch_csi`, 상태기계 GROUND/ESC/CSI/OSC, 청크 경계 넘어 상태 유지)로 재작성.
        CSI 커서이동(CUU/D/F/B·CUP·CHA·VPA)·ED/EL·SGR(16/256/트루컬러+bold/reverse)·CR/LF/BS/TAB(8)·
        autowrap·DECSC/RC·RI·DECTCEM(?25)·OSC 무시 지원. 데모 자식을 `powershell` 로 바꿔 프롬프트·
        16색·구문강조·`Clear-Host`(ED+CUP)가 정확히 렌더됨을 캡처로 확인(제어 시퀀스가 더 이상 글자로
        안 보임).
  - [x] **M3c — PTY 크기 동기화** — ConBox 에 리사이즈 싱크(`set_resize_sink`)와 그리드 getter
        (`grid_cols`/`grid_rows`)를 추가하고, `update_metrics` 가 cols/rows 변동 시 통지하도록 함.
        ConExe 가 `attach()` 에서 싱크를 등록해 `resize()`→`ResizePseudoConsole` 로 연결, 데모 start
        를 실제 그리드 크기로. 창을 좁히면 자식 콘솔 크기가 따라 바뀜을 캡처로 확인(`$Host.UI.RawUI.
        WindowSize` = 새 그리드 53×16). → **M3 핵심(셀 그리드+VT 파서+리사이즈) 완료.**
  - [ ] **M3a-2 — 마우스 드래그 선택/복사·더블클릭 단어선택** 을 그리드 좌표로 복구 (M3 부가, 독립)  ← **남은 작업**
- [x] **M4 — 풀스크린 지원** — 대체 화면(`?1049`/`?1047`/`?47`/`?1048`, `enter/leave_alt_screen`,
      스크롤백 동결)·스크롤 영역(DECSTBM `CSI r`, `scroll_top/bot`, LF/RI 재작성)·줄 삽입삭제
      (IL `CSI L`/DL `CSI M`)·문자 삽입삭제(ICH `@`/DCH `P`)·ECH `X`·SU `S`/SD `T` 구현.
      실전 풀스크린 TUI **vim** 이 ConBox 안에서 alt screen 진입/편집(dd 줄삭제)/종료 후 주 화면
      복원까지 정상 동작함을 캡처로 확인. → 정식 문서 이관 완료(요구 §2.5 · 설계 §1.2~1.4 · 개발노트 §2.3).
- [~] **M5 — claude.exe 구동 + 마감** — `<`/`>`/`=` 프라이빗 시퀀스 무시(`vt_gtlt`, kitty/XTMODKEYS
      오인 버그 수정)·DSR/DA 응답·특수키 보강(수정자·F1~F12·Shift+Tab·앱 커서 키 DECCKM)·bracketed
      paste(`?2004`)·한글 IME 조합 강제 확정(`finalize_composition`)·ConExe 자식 종료 콜백
      (`set_exit_callback`, 프로세스 핸들 폴링으로 감지) 구현. **claude.exe** 가 ConBox 안에서
      구동·입력·렌더됨을 캡처로 확인. 방향키 삽입(`1X23`)·종료 콜백 안내도 확인.
      → 남은 것: **한글 IME 조합 순서 실타이핑 수동 검증**(자동 캡처 불가, 개발노트 §1.8·§2.4).
        코드 점검은 완료(두 경로 모두 `[완성][트리거]` 보장, 이중전송 차단).

---

## 목표

단순 stdin/stdout 리다이렉트가 아니라 **claude.exe / vim / htop 같은 풀스크린 TUI까지
ConBox 안에서 제대로 구동**하는 것이 목표다.

## 확정 결정

| 항목 | 결정 |
|------|------|
| 자식 실행 백엔드 | ConPTY (`CreatePseudoConsole`) |
| ConBox 모델 | 셀 그리드 VT 에뮬레이터로 통합 (기존 append 모델 폐기/흡수) |
| 입력 | 키 raw → VT 인코딩 → PTY stdin |
| 출력 펌프 | 타이머 폴링(`PeekNamedPipe`→`ReadFile`)으로 시작, ConExe 내부 구현이라 후에 워커 스레드로 교체 가능 |
| ConExe ↔ ConBox | ConExe 생성 시 ConBox 지정, 입출력 자동 연결 |
| VT 파서 | 처음부터 필수 (ConPTY 출력 대부분이 VT 시퀀스) |
| 지원 목표 | claude.exe / vim / htop 풀스크린 TUI |

배경 보충:
- ConPTY 는 자식이 진짜 콘솔로 인식하므로 단순 라인 지향 ~ 풀스크린 TUI 가 한 경로로
  동작한다(에뮬레이터가 라인 지향을 포함하는 상위집합이라 이중 모드는 두지 않는다).
- ConPTY 는 stdout/stderr 을 단일 콘솔 출력으로 합친다. "화면에 보여주는" 용도라 적합하며,
  순수 데이터 파이프(출력을 프로그램적으로 가공)와는 다르다.
- 풀스크린은 raw 입력이라 에코·줄편집·커서를 자식이 VT 출력으로 그린다. 그래서 ConBox 의
  기존 입력 줄편집 / `on_enter` / IME 삽입 로직은 셀 그리드 모델로 흡수·축소된다.

## 단계별 로드맵 (각 단계 = 빌드·실행·검증 가능)

매 단계가 "실제 프로그램을 띄워 눈으로 확인"되는 **수직 슬라이스(tracer bullet)** 로
진행한다. 가장 위험한 셀 그리드 재설계(M3)는 ConPTY 왕복(M1~M2)이 검증된 뒤에 들어간다.

### M1 — ConPTY 왕복(출력 단방향)
- 신규 `Source/ConExe.h` / `ConExe.cpp` (클래스 `CConExe`):
  - 입력/출력 파이프 쌍 생성 → `CreatePseudoConsole` 로 PTY.
  - `STARTUPINFOEX` + `PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE_HANDLE` 로 자식 `CreateProcess`.
  - 출력 파이프를 UI 타이머(16~30ms)로 `PeekNamedPipe`→`ReadFile` 해 ConBox 에 전달.
- ConBox: 받은 UTF-8 바이트를 **임시로 기존 `print()`** 로 표시(VT 미해석).
- 검증: 데모에서 `cmd /c dir` 출력이 ConBox 에 뜬다. (기존 ConBox 거의 불변 → 위험 낮음)

### M2 — 입력 raw 전송 (왕복 완성)
- ConBox 키 입력(문자/Enter/Backspace/방향키/Ctrl 조합)을 VT 로 인코딩해 `CConExe::write`
  로 PTY 입력 파이프에 쓴다. 로컬 에코는 끈다(자식이 에코).
- 검증: `cmd` 또는 `python -i` 띄워 명령 입력 → 실행·결과 표시. (VT 파서 전이라 일부
  제어가 날것으로 보일 수 있음 — M3 에서 해소)

### M3 — 셀 그리드 화면 버퍼 + 코어 VT 파서 (가장 큰 단계)
- ConBox 내부를 `cols × rows` **셀 그리드 화면 버퍼**(셀 = 글자 + 색/속성)로 전환,
  커서를 `(row, col)` 절대 위치로 관리.
- VT 파서 상태기계: CSI 파싱, 커서 이동(CUP/CUU/CUD/CUF/CUB), 지우기(ED/EL),
  CR/LF/BS/TAB, SGR(기본 16색·굵기/반전 등), 폭 초과 자동 줄넘김.
- PTY 크기 동기화: 그리드 `cols/rows` 변화 → `ResizePseudoConsole`. 창 리사이즈 연동.
- 렌더링: OnPaint 를 셀 그리드 그리기로 교체(기존 셀 렌더·폰트·더블버퍼·커서 자산 재활용).
- 정리 대상: 기존 `log_buf`/`scr_lines`/입력 줄편집/`commit_input`/`on_enter`/IME 삽입 경로.
- 검증: PowerShell/cmd 에서 `cls`·색상 프롬프트·커서 이동 정확, 창 리사이즈 반영.
- 필요하면 **3a 그리드 전환 / 3b VT 코어 / 3c 리사이즈 동기화** 로 더 분할해 검증.

### M4 — 풀스크린 지원
- 대체 화면 버퍼(`?1049h/l`) + 주 화면 스크롤백.
- 스크롤 영역(DECSTBM), 줄/문자 삽입·삭제(IL/DL/ICH/DCH).
- 트루컬러/256색 SGR, 자주 쓰는 모드(커서 표시 `?25`, 자동 줄바꿈 등).
- 검증: `vim` / `htop`(또는 `more`)이 화면을 제대로 그리고 스크롤.

### M5 — claude.exe 구동 + 마감
- 특수키/조합키 VT 인코딩 보강, bracketed paste, (선택) 마우스 SGR 시퀀스.
- 한글 IME: 조합 완성분을 UTF-8 로 PTY stdin 전송(raw 모드 IME 처리).
- 자식 종료 감지 → `on_exit` 콜백, 리사이즈/포커스/정리 마감.
- 검증: **claude.exe** 가 ConBox 안에서 입력/렌더/스크롤/리사이즈 정상 동작.

## 영향 받는 파일

- 신규: `Source/ConExe.h`, `Source/ConExe.cpp` (UTF-8 BOM, vcxproj 에 추가·PrecompiledHeader=NotUsing,
  filters 는 `소스 파일`). 개발노트 §1.2 Source 관례 준수.
- 대폭 개편: `Source/ConBox.h`, `Source/ConBox.cpp` (M3~M5 — 셀 그리드/VT 파서/raw 입력).
- 데모: `Project/ConBoxDlg.cpp` (ConExe 로 자식 실행해 검증; 단계별로 `cmd`→`vim`→claude.exe).
- vcxproj: `Project/ConBox.vcxproj`(+`.filters`) 에 ConExe 추가.

## 마일스톤 완료 시 정식 문서로 이관

- 요구사양서 §3 ConExe: 공개 API·동작·키 입력 매핑·지원 범위. §1 의 "stdin/stdout/stderr
  리다이렉트" 문구는 ConPTY 함의에 맞게 "자식 콘솔 입출력(stdout/stderr 합쳐진 콘솔 출력)"
  으로 정리. ConBox §2 도 셀 그리드/VT 모델로 갱신.
- 설계명세서 §2 ConExe: ConPTY 흐름·타이머 펌프·VT 파서 상태기계·셀 그리드 데이터 모델.
- 개발노트 §2 ConExe: ConPTY Win10 1809+ 요구, 빌드/검증(자식 프로그램별 캡처 검증).

## 검증 방법 (공통)

- 빌드: 개발노트 §1.1 의 MSBuild 명령(x64 Debug).
- 실행 검증: 데모 앱을 PowerShell 로 띄우고 화면 캡처(개발노트 §1.4). 단계별 자식
  프로그램(`cmd /c dir` → `cmd`/`python -i` → PowerShell `cls`/색상 → `vim`/`htop` →
  `claude.exe`)으로 "그 프로그램이 제대로 보이고 조작되는가"를 합격 기준으로 삼는다.

## 진행 메모

- M3 가 위험이 가장 크므로, 착수 전 3a/3b/3c 분할 여부를 다시 판단한다.
- 전체를 한 세션에 끝내지 않는다. 각 단계 완료 시 결과를 정식 문서로 이관하고 위
  "진행상황" 체크리스트를 갱신한다.
