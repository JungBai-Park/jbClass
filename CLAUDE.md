# 프로젝트 개요

아래와 같은 역할을 하는 MFC 기반의 클래스 라이브러리 개발 프로젝트이다.

#### 1. FrameBox 모듈 :

- 대화 상자 컨트롤들의 레이아웃을 동적으로 이동하여 배치하고 이를 소스 코드에 반영한다.
- 대화 상자 컨트롤들의 입력을 while loop 내에서 절차 지향적으로 처리할 수 있는 제어 방식을 제공한다.

#### 2. ConBox 모듈 :

- VT100 호환 터미널 출력 화면 제공
- 차일드 프로세스를 실행하여, stdin/stdout 입출력 연동 (ConPTY 방식)
- 기존 터미널 프로그램들의 한글 입력 불편함 개선

#### 3. TableBox 모듈

- 엑셀과 유사한 느낌의 그리드 컨트롤 제공
- 멤버함수 오버로딩을 이용한 가상 그리드 방식 (화면에 보여지는 데이터만 실시간으로 요청하여 처리)



# 개발 환경

- Language: C++
- OS : Windows 10 64bit, 1809+
- 컴파일러 : Visual Studio 2022, MFC 기반, C++/MFC (Unicode 및 MBCS 호환)
- **MBCS 빌드 호환**:
  모든 MFC/Win32 호출은 명시적 W-접미사 변형(`::CreateFontIndirectW`, `::TextOutW`, `::GetObjectW`, `::CreateEnhMetaFileW`,
  `GetClassNameW`, `CreateWindowExW` 등)을 사용하여 프로젝트의 유니코드/멀티바이트 설정에 무관하게 컴파일 가능해야 한다.
- 텍스트 파일 인코딩 형태 :
  - 소스코드 `(*.c, *.cpp, *.h, *.rc, *.rc2)` :
    무조건 UTF-8 with BOM 형태로 저장한다. (생성한 경우 수정한 경우 모두)
  - 배치 파일 `(*.bat)` :
    무조건 CP949 형태로 저장한다. (생성한 경우 수정한 경우 모두)
  - 그 외 :
    새로 생성할 경우는 UTF-8 with BOM 형태로 인코딩 되어야 한다.
    기존의 파일을 수정할 경우는 원래 인코딩 상태와 동일하게 인코딩되어 저장해야 한다.




# 작업 방식

- 답변은 항상 격식체( "~합니다.", "~합니까?", "~입니다.", "~하겠습니다.")를 사용한다.
- 답변은 항상 한국어로 답한다.
- 작업과정, 추론과정, 생각하는 내용도 가급적 한국어로 표시한다.
- 특별한 사유가 없는 한, Debug|x64 형태로 빌드하여 시험한다.
- 무언가를 실행 시킬 때 PowerShell을 이용해도 되고 bash를 이용해도 되는 상황에서는,
  운영체제가 Windows 10 64bit인 점을 감안하여 PowerShell을 이용한다.
- 실행을 시켜서 동작을 확인하는 절차는 너무 과하게 하지 않는다.
  - 불확실성이 없는 소스 코드 수정 (당연히 코딩한 대로 동작하게 되는 경우)은 시험을 생략해도 좋다.
  - 용량이 큰 자료(화면 캡춰, 그림 파일 등)을 검토해야 하는 시험은 반드시
    계획에 시험 내용을 명시하여 사용자한테 알려야 한다.
- git에 저장할 때, 사용자의 명시적인 요청이 없는 한 브랜치를 만들지 않고 master에 바로 커밋한다.
- git에 저장할 때, 해당 세션 이외에서 수정된 파일이 있더라도 제외하지 말고 모두 커밋한다.
  이때, 해당 세션 이외에서 수정된 파일들의 변경사항까지 커밋에 기록하기 위해 파일의 변경 내용을 분석해야 한다.



# 폴더 구조

- 모든 소스코드는 반드시 ./Source 디렉토리 아래에 생성한다.
- 모든 임시파일은 반드시 ./Temp 디렉토리 아래에 생성한다. (Temp/가 없으면 만들어서 그 밑에 생성)
- 클로드 입장에서는 주어진 경로 밑의 작업이 모두 하나의 프로젝트이지만,
  Visual Studio 입장에서는 Build 가 솔루션 디렉토리이고, 그 밑에 여러개의 프로젝트가 있다.
- 사용자가 요청하는 내용의 문맥을 해석할 때 
  '~~ 프로젝트'라 함은 Visual Studio 입장에서의 프로젝트를 말하는 것으로 우선 판단한다.

```
project-root/
│
├── CLAUDE.md
│
├── Documents/
│    ├── REQUIREMENTS.md, PITFALLS.md, PROGRESS.md 등의 프로젝트 전체 공통 문서
│    ├── 1. FrameBox/   (REQUIREMENTS.md, PITFALLS.md)
│    ├── 2. ConBox/     (REQUIREMENTS.md, PITFALLS.md)
│    └── 3. TableBox/   (REQUIREMENTS.md, PITFALLS.md)
│
├── Build/
│    └── 비주얼 스튜디오 솔루션 디렉토리 : 솔루션(jbBox.sln)이 이 디렉토리 바로 밑에 위치.
│         프로젝트별로 Build/[프로젝트명]/ 서브디렉토리를 만들어 그 안에 배치한다.
│         (예: 데모 앱 - Build/DemoApp/, 신규 프로젝트 - Build/jbTerm/ 등)
│
├── Source/
│    └── 소스코드 폴더 : 솔루션에 속한 프로젝트들이 공용으로 사용하는 소스코드들은 여기에
│
├── Reference/
│    └── 단순 참고용 문서 : 클로드가 작업 시 이 폴더의 내용까지 확인해 볼 필요는 없음. (토큰 절약)
│
└── Temp/
     └── 임시 파일 폴더 : 테스트 스크립트, 캡처 이미지, 측정용 ps1, 실험 코드 등
```

- 빌드 디렉토리 설정 (각 프로젝트의 구성 속성 -> 일반) :
  ```
  모든 구성 of x64
  - 출력 디렉터리 : $(ProjectDir)$(Configuration).64\
  - 중간 디렉터리 : $(Configuration).64\obj\
  
  모든 구성 of Win32
  - 출력 디렉터리 : $(ProjectDir)$(Configuration).32\
  - 중간 디렉터리 : $(Configuration).32\obj\
  ```



# 활성 서브 프로젝트

- 지금은 jbTerm 프로젝트를 개발하고 있으므로, 특별히 어떤 프로젝트를 특정하지 않고
  기능 구현을 요청한다면, jbTerm에 대한 기능 구현으로 생각하면 된다.
  - 즉, Build\jBTerm 디렉토리의 파일을 수정하여 요청한 내용을 처리한다.
  - Source 폴더 내의 코드 수정이 필요한 경우, 타 프로젝트에 대한 영향성을 검토하여 사용자에게 보고한 후, 승인을 받고 진행할 것.



# 주요 문서

※ 마크다운 문서는 UTF-8 with BOM 형태로 작성한다.
※ 마크다운 문서 작성시 문장이 길어져도 줄바꿈문자를 삽입하지 않고 작성한다. 
   (Viewer가 자동으로 줄바꿈하는 것에 의존해야 함)

#### 1. 요구사양서 (REQUIREMENTS.md)

@Documents/REQUIREMENTS.md

- /organize-docs 스킬 발동 시, 세션 내용 중에서 사용자가 추가 요청한 내용들을 정리하여 이 문서에 반영한다.
- 사용자가 직접적으로 요청한 내용들을 이 문서에 반영하고, 요청한 내용에 따라 결정된 사항들은 소스 코드의 코멘트에 기록해 둔다.
- 요구사양서는 사용자가 보기 위한 문서가 아니라 ...
  매 세션 마다 AI가 업무를 파악하기 위한 용도이므로, 군더더기 없이 (과하지 않게) 영어로 작성한다.
  (AI에게 도움되지 않는 것은 적지 않는다.)
- 모듈별 요구사양은 Documents/[모듈명]/ 서브디렉토리의 REQUIREMENTS.md에 기록하며,
  해당 세션에서 작업한 모듈의 것만 읽는다.

#### 2. 설계명세서

- 설계명세서는 별도로 만들지 않는다. (토큰 절약 목적)
- /organize-docs 스킬 발동 시, 설계명세서에 적을 내용을 소스코드의 코멘트에 영어로 반영해 둔다. (세부 내용은 `코딩 규칙` 참조)

#### 3. 개발 노트 (PITFALLS.md)

@Documents/PITFALLS.md

- /organize-docs 스킬 발동 시, 세션 내용 중에서 기억해 두어야 할 내용들을 정리하여 이 문서에 반영한다.
  - 예 : 디버깅 과정 중 알게 된 특성, 시행착오 내용 (똑같은 실수를 반복하지 않기 위해)
- 자료 관리의 편의를 위해  ~/.claude의 메모리 시스템을 절대 사용하지 말고 반드시 Documents/PITFALLS.md 에 기록한다.
- 개발 노트는 사용자가 보기 위한 문서가 아니라 ...
  매 세션 마다 AI가 잊지 말아야 할 내용을 상기시켜 주기 위한 용도이므로, 군더더기 없이 (과하지 않게) 영어로 작성한다.
  **즉, AI에게 도움 되지 않는 것은 적지 않는다.**
- 모듈별 개발 노트는 Documents/[모듈명]/ 서브디렉토리의 PITFALLS.md에 기록하며,
  해당 세션에서 작업한 모듈의 것만 읽는다.

#### 4. 진척 현황 (PROGRESS.md)

- 최종 개발 목표를 한번에 완성 시키지 않고, 단계별로 계획을 세워 진행할 경우 이 문서를 작성한다.
  (이 문서가 존재하지 않는다면, 현재는 단계별로 추진하고 있는 목표가 없는 안정된 상태로 생각하면 된다.)
- /organize-docs 스킬 발동 시, 이 문서가 있다면 단계별 계획의 진척 상황을 문서에 반영한다.
  - 완료한 단계는 완료되었다고 표시한다.
  - 완료된 단계의 세부 내용은 토큰 절약을 위해 간략화 또는 생략한다.

#### 5. Andrei Karpathy의 LLM 코딩 원칙

@Documents/Karpathy.md

#### 6. jbBox Class Library 매뉴얼 (Documents/jbBox Class Library 매뉴얼.md)

- 이 문서를 배포받는 대상은 **개발자**이다 (Class Library를 가져다 쓰는 입장).
- 개발자 입장에서의 사용법, 빌드 절차, 소스 코드 구조와 각 모듈의 역할, 주의사항, 호환성 등을 담는다.

#### 7. jbTerm 프로그램 매뉴얼 (Documents/jbTerm 프로그램 매뉴얼.md)

- 이 문서를 배포받는 대상은 **일반 Windows 사용자**이다 (jbTerm.exe만 실행해서 쓰는 입장이며, 개발자가 아니다).
- 이 프로그램을 충분히 활용하기 위해 사용자가 알아야 할 사항만 담는다.
- 다음 내용은 적지 않는다 :
  - 빌드 방법, 개발 환경 등 개발자용 정보.
  - "이전 버전에서는 이랬는데 지금은 이렇게 바뀌었다"는 식으로 과거 버전 대비 기능 변경 이력을
    서술하는 내용. (jbTerm은 아직 기능이 수시로 바뀌는 단계이므로, 현재 시점의 동작만 서술한다.)



# 코딩 규칙

#### 1. 문자열 처리 함수

- `TCHAR` 매크로 또는 컴파일러 옵션에 따라 변동되는 매크로에 의존하지 않고 유니코드 API 함수( `...W`)를 명시적으로 호출한다.
- 유니코드 API 함수를 사용하더라도 `멀티바이트` 코드 용 컴파일러 구성에서 정상적으로 빌드 될 수 있도록 작성한다.
- 내부 변수 및 함수와 연동하는 모든 문자열은 UTF-8로 인코딩된 형태로 사용한다.
- 문자열을 다루기 위한 변수는 char *, const char *, std::string 형식으로 표현한다.
- API와 연동하기 위한 목적 이외에는 다른 형식(wchar *, const char *, CString 등)을 사용하지 않으며,
  다른 형식의 문자열이 사용되더라도 호출 직전/직후에 한해 임시적으로 사용한다.
- ''유니코드 API 함수를 명시적으로 사용한다."는 원칙에 따라,
  컴파일러 옵션에 따라 변동되는 매크로 (`_T()`, `LPCTSTR`, `TCHAR`, `CString` 등)은 사용하지 않는다.

#### 2. 주석

- 주석은 인간이 소스코드를 파악하기 위해 적어 놓은 것이 아니라, AI가 소스코드를 잘 활용할 수 있도록 도움을 주는 목적으로 적는다.
  (AI에게 도움되지 않는 것은 적지 않는다. 예외 : Copyright 문구)
- 토큰 절약과 인코딩으로 인한 에러를 방지하기 위해, 모든 코멘트는 영어로 적는다.
- 공개 인터페이스 헤더(.h)에는 용도·의존성/요구사항·최소 사용 예제를 담아, 
  "사용만" 할 때는 그 헤더만 읽으면 충분하도록 자기완결적으로 작성한다.
- 소스코드 전체를 읽지 않아도 되는 상황에서 불필요하게 읽히지 않도록 토큰 효율적인 구조로 정리한다.
- 바이트 값 128이상 사용 금지하며 ASCII 문자만 사용한다.

  - 단, 이 금지 조항은 오직 주석에만 적용하며, 소스 코드에 한글 문자열이 포함되어야 할 경우에는 한글(UTF-8)로 적는다.
- 소스코드를 보면 알 수 있는 내용은 굳이 코멘트로 적지 않는다.
- 설계 의도, 동작 방식, 사용 시 주의해야 할 점과 같은 내용을 상세히 적는다.
- 중요 :
  PROGRESS.md에 기술된 단계별 계획에 따라 개발이 진행되더라도,
  소스코드 주석에는 Stage 번호 및 단계별 계획에 대한 내용을 기록하지 않는다. (개발 완료된 후 찾아서 지워야 함)
  개발과정이 아니라 개발완료 후 소스코드를 AI가 사용하는 입장에서 필요한 정보만 기록한다.




#### 3. 명명 규칙

- 파일이름 :
  - PascalCase, 확장자 및 쩜 제외한 이름은 16자 이내 (예: MainCtrl.c)
- 글로벌 함수 :
  - PascalCase, 대문자로 시작 (예: FunctionNameSample, MyClassName)
  - 가급적 두 단어 이상으로 조합된 이름을 사용한다.
- 글로벌 변수 :
  - camelCase, 소문자로 시작 (예: functionNameSample, myVariableName)
  - 단, 반드시 대문자가 있어야 하기에, 반드시 두 단어 이상으로 조합된 이름을 사용한다.
- 멤버변수, 멤버 함수, 로컬 변수 :
  - snake_case, 소문자 + 언더바 (function_name_sample)
  - 단, 로컬변수는 가급적 언더바 없이 짧게 한단어로 표현한다. (정확한 의미 전달이 필요한 경우는 예외로 둔다.)
  - 단순 루프제어의 iteration 변수는 i, n, k 등의 1개 소문자 변수를 사용한다.
  - 멤버 변수를 뜻하는 접두어(m_)는 사용하지 않는다.
- 매크로 및 enum 상수 :
  - 모두 대문자를 이용하고 여러단어를 조합할 경우 언더바(_)를 사용한다.
- 단, 개발환경(MFC 등)과의 호환을 위해 불가피한 경우는 예외로 둔다.

#### 4. 메시지 맵 매크로 배치 (CBM 인덱싱 대응)

- MFC의 `BEGIN_MESSAGE_MAP(...) ~ END_MESSAGE_MAP()` 블록은 전처리기 확장 전 형태가
  일반 C++ 문법과 달라, CBM(codebase-memory-mcp) 인덱서의 파서가 이 지점부터 파일 뒤쪽까지
  넓은 범위를 잘못 처리하는 문제를 일으킬 수 있다.
- 해당 클래스 정의가 끝난 뒤, 같은 `.cpp` 파일의 맨 끝(다른 모든 함수 정의보다 뒤)에 배치한다.
  (클래스가 이미 완전히 선언되어 있으면 파일 내 위치와 무관하게 정상 동작한다.)
- `DECLARE_MESSAGE_MAP()`은 클래스 본문 안에 있어야 하므로 이동 대상이 아니며, 헤더의
  클래스 선언부에 그대로 둔다.
